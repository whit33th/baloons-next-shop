# 🎈 Ballonique - Инструкция по финальной настройке

## ✅ Что уже сделано

### 1. Обновлены категории товаров

- ✅ Детские
- ✅ Для неё
- ✅ Для него
- ✅ Кохання
- ✅ Мама
- ✅ Рождение ребёнка
- ✅ Сюрприз в шаре
- ✅ Юбилей
- ✅ Букеты из шаров
- ✅ Для любого события

### 2. Реализован платёжный флоу

- ✅ Полная онлайн-оплата через Stripe
- ✅ Предоплата 30% онлайн
- ✅ Оплата наличными с обязательным WhatsApp подтверждением

### 3. Добавлена информация о доставке

- ✅ Страница `/delivery` с полной информацией
- ✅ Интеграция с WhatsApp для подтверждения заказов
- ✅ Двуязычная поддержка (русский/немецкий)

## 🔧 Что нужно настроить

### ШАГ 1: Настроить номер WhatsApp

Откройте файл `lib/config.ts` и замените:

```typescript
export const WHATSAPP_NUMBER = "YOUR_WHATSAPP_NUMBER";
```

на ваш реальный номер, например:

```typescript
export const WHATSAPP_NUMBER = "436641234567";
```

**Важно:** Формат без знака + и пробелов!

### ШАГ 2: Добавить иконки категорий

Создайте иконки (рекомендуемый размер 48x48px) и сохраните в папку `/public/icons/`:

```
public/
  icons/
    ├── kids.png          (Детские)
    ├── for-her.png       (Для неё)
    ├── for-him.png       (Для него)
    ├── love.png          (Кохання)
    ├── mom.png           (Мама)
    ├── baby.png          (Рождение ребёнка)
    ├── surprise.png      (Сюрприз в шаре)
    ├── anniversary.png   (Юбилей)
    ├── bouquet.png       (Букеты из шаров)
    └── any-event.png     (Для любого события)
```

Временно можно использовать любые изображения для тестирования.

### ШАГ 3: Настроить Stripe (для онлайн-оплаты)

1. Зарегистрируйтесь на [stripe.com](https://stripe.com)
2. Получите API ключи (Publishable key и Secret key)
3. Создайте файл `.env.local` в корне проекта:

```env
# Stripe Keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...

# Convex (если еще не настроено)
NEXT_PUBLIC_CONVEX_URL=...
CONVEX_DEPLOYMENT=...
```

4. Создайте обработчик Stripe webhook в `app/api/webhooks/stripe/route.ts`:

```typescript
import { headers } from "next/headers";
import Stripe from "stripe";

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);

export async function POST(req: Request) {
  const body = await req.text();
  const signature = headers().get("stripe-signature")!;

  try {
    const event = stripe.webhooks.constructEvent(
      body,
      signature,
      process.env.STRIPE_WEBHOOK_SECRET!,
    );

    // Handle payment success
    if (event.type === "payment_intent.succeeded") {
      const paymentIntent = event.data.object;
      // Process successful payment
      // Call your Convex function to update order
    }

    return new Response("OK", { status: 200 });
  } catch (err) {
    return new Response("Webhook Error", { status: 400 });
  }
}
```

### ШАГ 4: Обновить конфигурацию магазина (опционально)

Откройте `lib/config.ts` и проверьте/измените настройки:

- Адрес магазина
- Часы работы
- Стоимость доставки
- Сроки подготовки заказа
- Правила отмены

## 🚀 Запуск проекта

```bash
# 1. Установка зависимостей
pnpm install

# 2. Запуск Convex backend
pnpm convex dev

# 3. В другом терминале - запуск Next.js
pnpm dev
```

Сайт будет доступен на `http://localhost:3000`

## 📝 Тестирование

### Проверьте следующие сценарии:

1. **Категории**
   - Перейдите в каталог
   - Убедитесь, что все категории отображаются корректно

2. **Оформление заказа - Самовывоз + Наличные**
   - Добавьте товар в корзину
   - Перейдите к оформлению
   - Выберите "Самовывоз"
   - Выберите "Оплата наличными"
   - Нажмите кнопку WhatsApp - должно открыться приложение с сообщением
   - После этого кнопка "Оформить заказ" должна активироваться

3. **Оформление заказа - Самовывоз + Онлайн оплата**
   - Выберите "Полная онлайн-оплата"
   - Система должна перейти к Stripe checkout

4. **Оформление заказа - Доставка**
   - Выберите "Доставка"
   - Убедитесь, что опция "Оплата наличными" недоступна

5. **Страница доставки**
   - Перейдите на `/delivery`
   - Проверьте отображение информации
   - Проверьте работу кнопки WhatsApp

## 🔍 Структура обновленных файлов

```
app/
  checkout/page.tsx          ✏️ ИЗМЕНЕН (новый платежный флоу)
  delivery/page.tsx          ✨ СОЗДАН (информация о доставке)

components/
  Containers/
    ProductFilters/
      ProductFilters.tsx     ✏️ ИЗМЕНЕН (новые категории)

convex/
  schema.ts                  ✏️ ИЗМЕНЕН (новые поля заказа)
  orders.ts                  ✏️ ИЗМЕНЕН (поддержка типов оплаты)
  paymentMutations.ts        ✏️ ИЗМЕНЕН (частичная оплата)

lib/
  config.ts                  ✨ СОЗДАН (централизованная конфигурация)

IMPLEMENTATION_NOTES.md      ✨ СОЗДАН (документация)
SETUP_GUIDE.md              ✨ СОЗДАН (этот файл)
```

## ❓ Частые вопросы

**Q: Как изменить список категорий?**
A: Откройте `lib/config.ts` и измените массив `CATEGORIES`

**Q: Как изменить стоимость доставки?**
A: Откройте `lib/config.ts` и измените `STORE_INFO.delivery.cost`

**Q: Как добавить другие языки?**
A: Добавьте переводы в `lib/config.ts` и обновите компоненты

**Q: WhatsApp не открывается**
A: Убедитесь, что номер указан правильно и WhatsApp установлен

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте логи Convex dashboard
3. Убедитесь, что все зависимости установлены: `pnpm install`

## 📧 Контакты

Для вопросов по настройке обращайтесь к разработчику проекта.

---

**Успешного запуска! 🎈**
